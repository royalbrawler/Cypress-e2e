describe('Docket Post Test', () => {
    it('Random User', () => {
        cy.task('freshUser').then(obj => {
            cy.log(obj)
        })
    })
    it('Random User', () => {
        cy.task('freshUser').then(obj => {
            cy.log(obj)
        })
    })
    it('Random User', () => {
        cy.task('freshUser').then(obj => {
            cy.log(obj)
        })
    })
    it('Random User', () => {
        cy.task('freshUser').then(obj => {
            cy.log(obj)
        })
    })
})
